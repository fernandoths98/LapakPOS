<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Camera access (Phase 3a) — react-native-vision-camera's
      useCameraPermission()/requestPermission() in BarcodeScanner.tsx (Sell's
      barcode scan, Product's "Scan barcode" and photo capture for "Add
      photo"/"Snap to fill") drives Android's runtime permission prompt, but
      that prompt only exists if the permission is declared here — without
      this line requestPermission() would always resolve to denied on
      Android. This was missing until Phase 7 caught it as a pre-existing gap.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      Bluetooth thermal-printer support (Phase 7, Android only — see
      mobile/src/lib/bluetoothPrinter/index.ts for why iOS is out of scope).
      react-native-bluetooth-classic's own AndroidManifest already declares
      exactly these five lines (verbatim) and they'd get merged in
      automatically by Gradle either way — they're spelled out here too,
      identically, so anyone reading this manifest can see the app's real
      permission surface without digging into node_modules, and so there's
      nothing for Gradle's manifest merger to reconcile (mismatched
      attributes on a duplicate <uses-permission> across manifests is a
      build-time merge conflict, so this intentionally matches the
      library's own declaration rather than tightening it independently).

      BLUETOOTH / BLUETOOTH_ADMIN cover classic-Bluetooth connect+write on
      Android ≤11 (API ≤30) — normal, install-time permissions there, so no
      runtime prompt is needed pre-12. ACCESS_FINE_LOCATION is required to
      enumerate/discover Bluetooth devices at all on those same older
      versions (a long-standing Android platform quirk that treats
      nearby-device scanning as a location signal). Android 12+ (API 31+)
      replaced that pair with BLUETOOTH_SCAN/BLUETOOTH_CONNECT as their own
      dangerous, runtime-requestable permissions instead —
      requestBluetoothPermissions() in bluetoothPrinter/index.ts branches on
      API level and requests whichever pair applies.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:minSdkVersion="31" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
