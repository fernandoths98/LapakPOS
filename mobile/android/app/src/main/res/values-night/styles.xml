<resources>

    <!-- Base application theme. The app ships a single, carefully-tuned light
         palette (mobile/src/theme/tokens.ts); it has no dark design, so we
         render the intended light UI regardless of the system setting rather
         than letting Android force-dark invert it into a muddy mess. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>

</resources>
